type Post {
  id: ID! @id
  published: Boolean! @default(value: false)
  title: String!
  content: String!
  user: User
}

type User {
  id: ID! @id
  email: String! @unique
  password: String!
  posts: [Post!]!
}